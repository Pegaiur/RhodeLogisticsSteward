# Inbox

待办事项，不阻塞当前功能，触发条件满足时启动。

---

### Strategy 策略组合器

**触发**：`SolverConfig` bool 开关 ≥ 4 个时

当前独立开关无法表达策略间互锁（如"全局评分仅在独占检查开启时生效"）或元策略（"同时跑两种算法取最高分"）。方向：引入 `Strategy` 抽象层封装预处理+求解+后处理+目标函数的完整流水线。

### 瓶颈枚举（互补件一）

**触发**：跨 Phase 资源分配成为显著瓶颈时（如"黑键该去 Mfg 支撑还是 Trade 主力"）

识别 8-12 个关键瓶颈干员，枚举可行分配方案取全局最优。与全局状态评分互补。

### 局部搜索策略化

**触发**：当前 first-improvement 策略不够用

支持 best-improvement / simulated annealing / 加权搜索。
